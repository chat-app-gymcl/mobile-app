import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'tk.gymcl.mobile-app',
  appName: '2AV',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
